{
  "name": "koi-monorepo",
  "private": true,
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "conformance": "turbo run conformance"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
