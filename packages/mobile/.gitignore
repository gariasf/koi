# CNG (D-048): the native projects are generated by `expo prebuild`, never
# committed — local builds only, no EAS.
/ios
/android

# Expo
.expo/
expo-env.d.ts
.metro-health-check*

# native junk
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
